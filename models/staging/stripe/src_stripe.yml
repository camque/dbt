version: 2

sources:
  - name: stripe
    database: kq_raw
    schema: stripe
    freshness:
      error_after:
        count: 3
        period: day
      warn_after:
        count: 2
        period: day
    loaded_at_field: _batched_at
    tables:
        - name: payment
          description: A payment record